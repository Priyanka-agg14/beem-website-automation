[pytest]
# Fixed the --tracing flag below
addopts = --headed --screenshot=only-on-failure --video=retain-on-failure --tracing=retain-on-failure --html=reports/html_report/report.html --self-contained-html --alluredir=reports/allure_results

# Define test discovery patterns
testpaths = tests
python_files = test_*.py


